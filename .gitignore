# Dependencias (se instalan con "npm install")
node_modules/

# Datos generados por la app (contienen credenciales/datos de clientes)
config.json
armados.json
catalogo.json
clientes_cache.json
buzon-sa.json
temp_etiquetas/

# Respaldos y temporales de esos mismos archivos (escritura atómica)
*.json.bak
*.json.tmp
*.json.dañado-*

# Build / empaquetado
dist/
bin/
*.exe

# Archivos de sistema
.DS_Store
Thumbs.db

dist/.verificacion-*

# Supervisor que viaja en el paquete (contrasena)
supervisor-inicial.json
usuario-inicial.json
